{
  "name": "cyprees-advanced",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open:dev": "cypress open --config-file config-dev.ts --browser chrome",
    "cypress:open:prod": "cypress open --config-file config-prod.ts --browser chrome"
  },
  "devDependencies": {
    "cypress": "^15.8.2",
    "cypress-mochawesome-reporter": "^4.0.2",
    "cypress-multi-reporters": "^2.0.5",
    "mocha-junit-reporter": "^2.2.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
